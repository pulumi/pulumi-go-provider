name: test
runtime: yaml

plugins:
  providers:
    - name: test
      path: .

resources:
  team:
    type: test:index:Team
    properties:
      slug: b
    options:
      ignoreChanges:
        - members
