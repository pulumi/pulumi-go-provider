run:
  timeout: 10m
issues:
  # Enable checking the by default skipped "examples" dirs
  exclude-dirs-use-default: false
  exclude-dirs:
    - vendor$
    - third_party$
    - testdata$
    - Godeps$
    - builtin$
linters:
  enable-all: false
  enable:
    - errcheck
    - goconst
    - gofmt
    - revive
    - gosec
    - gosimple
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - unconvert
    - paralleltest
    - staticcheck
    - stylecheck
    - unused
  disable:
